# Copy to `.env` and fill in from your EmailJS dashboard (Account → API Keys).
# These are public by design — EmailJS expects them in the client bundle — so
# enable domain allowlisting in the dashboard to protect your send quota.
VITE_PUBLIC_KEY=
VITE_SERVICE_ID=
VITE_TEMPLATE_ID=
